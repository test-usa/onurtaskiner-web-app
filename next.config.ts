import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["discoverholidaysbd.com"], // Add the domain where your images are hosted
  },
  // Other configuration options can go here
};

export default nextConfig;
